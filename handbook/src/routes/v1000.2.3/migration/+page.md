---
title: v1000.2.2 → v1000.2.3
description: Migrating from Beacon v1000.2.2 to v1000.2.3
---

v1000.2.2 → v1000.2.3. Patch release. CI and repository configuration changes. No API changes.

## What changed

- CI workflow restructured
- `.gitignore` and `.gitattributes` cleanup
- `.npmrc` added

No source code was modified. All existing code works without changes.

## Upgrade

```bash
npm install @nerdalytics/beacon@1000.2.3
```

No code changes required.
